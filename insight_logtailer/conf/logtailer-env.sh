
# 设置dm303的端口，默认是2358
LOGTAILER_DM303_PORT=11720

# 设置jmx的端口，默认是2359
LOGTAILER_JMX_PORT=11719

# 设置额外的放在java CLASSPATH上的路径或者jar
# 在使用inline模式，inline的类及其依赖的类需要放在这里指定
# 要如下制定jar包所在目录，否则stream*.properties的Inline无法找到需要调用的jar包
#APP_EXT_CLSPATH=/home/dev/code/test.jar
